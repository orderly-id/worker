{
  "type": "object",
  "properties": {
    "action": {
      "type": "string",
      "enum": [
        "create_note",
        "create_folder",
        "rename_note",
        "delete_note",
        "move_note",
        "rename_folder",
        "delete_folder",
        "mark_note_important",
        "unmark_note_important",
        "archive_note",
        "restore_note",
        "mark_folder_important",
        "unmark_folder_important",
        "archive_folder",
        "restore_folder",
        "list_notes",
        "answer",
        "clarify"
      ]
    },
    "note_id": { "type": "string" },
    "folder_id": { "type": "string" },
    "title": { "type": "string" },
    "new_title": { "type": "string" },
    "content": { "type": "string" },
    "folder": { "type": "string" },
    "new_folder": { "type": "string" },
    "folder_resolution": {
      "type": "string",
      "enum": ["explicit", "inferred", "default", "not_applicable"]
    },
    "confidence": { "type": "number", "minimum": 0, "maximum": 1 },
    "reply": { "type": "string" }
  },
  "required": ["action", "reply"]
}
